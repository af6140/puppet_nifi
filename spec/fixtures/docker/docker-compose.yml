version: "3"
services:
  nifi-as01a:
    hostname: nifi-as01a.docker_nificluster
    container_name: nifi-as01a
    image: c7-nifi-puppet:1.2
    ports:
      - "18081:8080/tcp"
    networks:
      - nificluster
    volumes:
      - ./ssl:/certs/:ro
      - ../../..:/modules/nifi:ro
  nifi-as02a:
    hostname: nifi-as02a.docker_nificluster
    container_name: nifi-as02a
    image: c7-nifi-puppet:1.2
    ports:
      - "18082:8080/tcp"
    networks:
      - nificluster
    volumes:
      - ./ssl:/certs/:ro
      - ../../..:/modules/nifi:ro
  nifi-as03a:
    hostname: nifi-as03a.docker_nificluster
    container_name: nifi-as03a
    image: c7-nifi-puppet:1.2
    ports:
      - "18083:8080/tcp"
    networks:
      - nificluster
    volumes:
      - ./ssl:/certs/:ro
      - ../../..:/modules/nifi:ro

networks:
  nificluster:

